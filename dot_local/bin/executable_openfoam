#!/bin/bash

podman run --rm -it \
    --userns=keep-id \
    --security-opt label=disable \
    --network=none \
    -v "$PWD:/home/openfoam" \
    -w /home/openfoam \
    docker.io/opencfd/openfoam-default:latest \
    bash

