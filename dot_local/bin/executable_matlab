#!/bin/bash

current_dir="$PWD"

distrobox enter ubuntu-tools -- bash -lc "
cd \"$current_dir\" || exit

export QT_QPA_PLATFORM=xcb
export GDK_BACKEND=x11
export _JAVA_AWT_WM_NONREPARENTING=1

exec /opt/MATLAB/R2026a/bin/matlab -desktop
"
