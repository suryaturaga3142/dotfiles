#!/usr/bin/env bash

MIC=$(wpctl get-volume @DEFAULT_AUDIO_SOURCE@)
ICON=""

if [[ "$MIC" == *"[MUTED]"* ]]; then
    ICON=" 󰍭 "
else
    ICON=" 󰍬 "
fi

echo "{ \"text\": \"${ICON}\", \"tooltip\": \"${MIC}\" }"

exit 0

